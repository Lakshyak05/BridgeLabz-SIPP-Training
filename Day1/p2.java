//package Day1;////public class p2 {//}/*class Member:    constructor(name, memberID, membershipDate):        set this.name to name        set this.memberID to memberID        set this.membershipDate to membershipDate    method getMemberDetails():        return name, memberID, membershipDate    method isMembershipValid():        currentDate = getCurrentDate()        membershipDuration = calculateDateDifference(membershipDate, currentDate)        if membershipDuration < 1:            return "Valid"        else:            return "Expired"    method calculateDateDifference(startDate, endDate):        return endDate.year - startDate.year    method extendMembership():        membershipDate = addYears(membershipDate, 1)        print("Membership extended by 1 year.")// Main programstart:    member1 = Member("Alice Johnson", "M123", "2023-08-14")    print(member1.getMemberDetails())    validity = member1.isMembershipValid()    print(validity)    member1.extendMembership()end */